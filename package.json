{
  "name": "jest-sonar-57",
  "scripts": {
    "test": "npm run test --ws --if-present"
  },
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "jest": "^29.0.3",
    "jest-sonar": "^0.2.12"
  }
}
